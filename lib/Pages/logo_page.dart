import 'package:flutter/material.dart';

class LogoPG extends StatefulWidget {
  const LogoPG({Key? key}) : super(key: key);

  @override
  State<LogoPG> createState() => _LogoPGState();
}

class _LogoPGState extends State<LogoPG> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("LOGO PAGE"),),
    body: Column(children: [],),);
  }
}
